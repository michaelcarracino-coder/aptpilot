# Dependencies
node_modules

# Build output
dist

# Secrets — never commit
.env
.env.*
!.env.example

# Vercel local link
.vercel

# macOS
.DS_Store

# Claude Code: keep the shared launch config, ignore local state
.claude/*
!.claude/launch.json
